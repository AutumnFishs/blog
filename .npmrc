# 设置npm源（优先级高于全局）
registry=https://mirrors.tencent.com/npm/

# 自动安装缺少的依赖，npm7版本后，默认为false
auto-install-peers=true 

# 将所有依赖项提升到最顶层的 node_modules 目录中
; shamefully-hoist=true

# # 禁用严格的 peer 依赖检查
; strict-peer-dependencies=false

